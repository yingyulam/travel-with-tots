Flask>=3.0
Werkzeug>=3.0
MarkupSafe>=2.1
requests>=2.31
python-dotenv>=1.0
gunicorn>=23.0

# Retrieval for the chatbot. The vector store is local; the embeddings are not.
# Running the model here cost 208MB resident to search 12KB of text, and never
# finished loading inside a request on a shared CPU -- see src/rag.py. tiktoken
# counts tokens for the model that does the embedding, at about 46MB.
chromadb>=0.5
tiktoken>=0.7

# The LLM calls.
langgraph>=0.2
langchain-core>=0.3
langchain-openai>=0.2
openai>=1.0

# Optional, and only for the Supabase data source on /settings: `supabase`
# clones the local database up (REST), `psycopg` is what serves pages from it
# (SQL). The app runs on local SQLite without either.
supabase>=2.0
psycopg[binary]>=3.1
