Flask>=3.0
requests>=2.31
python-dotenv>=1.0
sentence-transformers>=3.0
chromadb>=0.5
langgraph>=0.2
langchain-openai>=0.2
openai>=1.0
# Optional: only needed to clone the local database into Supabase.
# The app runs on SQLite without it.
supabase>=2.0
